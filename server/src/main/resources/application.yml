spring:
  application:
    name: server

## 유레카 서버 포트 설정
server:
  port: 19090

## 유레카 디폴트 존 설정
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:19090/eureka/
  instance:
    hostname: localhost
