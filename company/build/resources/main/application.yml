spring:
  application:
    name: company
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: root
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
server:
  port: 19094